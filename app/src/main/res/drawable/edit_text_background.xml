<!-- En res/drawable/edit_text_background.xml -->

<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del EditText -->
    <solid android:color="#daeeFf" />

    <!-- Borde del EditText -->
    <stroke
        android:width="2dp"
    android:color="@color/azul_logo" />

    <!-- Esquinas redondeadas -->
    <corners android:radius="8dp" />

    <!-- Padding interno de 16dp -->
    <padding
        android:left="16dp"
        android:right="16dp"/>
</shape>
